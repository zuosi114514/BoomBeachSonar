# 统一文本文件行尾为 LF，避免跨平台 CRLF/LF 混乱
* text=auto eol=lf

# Windows 批处理必须用 CRLF
*.bat text eol=crlf

# 二进制文件不做行尾转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.zip binary
